extends ../jade/bare

block pageId
  - var pageId = 'maintenance';

block append styles
  +style('ext.css')

block middle
  h1 #{l.titMaintenance}
  p
    | #{l.guiMaintenanceMessage.start}
    | #{siteTitle}
    | #{l.guiMaintenanceMessage.end}
